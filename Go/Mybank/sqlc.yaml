version: 1
packages:
    - path: "./db/sqlc"
      engine: "postgresql"
      name: "db"
      queries: "./db/query/"
      schema: "./db/migration/"

      emit_json_tags: true
      emit_prepared_queries: false
      emit_interface: false
      emit_exact_table_names: false
